/*
import { isValidHttpURL } from './url';

describe('isValidHttpURL', () => {
  it('should return true for valid URLs', () => {
    expect(
      isValidHttpURL(
        'https://download.data.grandlyon.com/wfs/grandlyon?SERVICE=WFS&VERSION=2.0.0&request=GetFeature&typename=eco_economie.ecomarcheinstance_latest&outputFormat=application/json;%20subtype=geojson&SRSNAME=EPSG:4171&startIndex=0'
      )
    ).toBe(true);
    expect(isValidHttpURL('https://github.com/datatlas-erasme/datatlas')).toBe(true);
  });

  it('should return false invalid URLs', () => {
    expect(isValidHttpURL('javascript:alert(23)')).toBe(false);
    expect(isValidHttpURL('1')).toBe(false);
  });
});*/
