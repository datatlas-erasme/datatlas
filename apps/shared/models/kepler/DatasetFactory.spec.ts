// import { DatasetFactory } from './DatasetFactory';

describe('DatasetFactory.createFromURL()', () => {
  it('Should return a proto dataset.', async () => {
    // const dataset = await DatasetFactory.createFromURL(
    //   new URL(
    //     'https://download.data.grandlyon.com/wfs/grandlyon?SERVICE=WFS&VERSION=2.0.0&request=GetFeature&typename=eco_economie.ecomarcheinstance_latest&outputFormat=application/json;%20subtype=geojson&SRSNAME=EPSG:4171&startIndex=0'
    //   )
    // );
    // expect(dataset).toHaveProperty('data.fields');
    // expect(dataset).toHaveProperty('data.rows');
  });
});
